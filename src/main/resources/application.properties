spring.application.name=ChessGame

# 数据库
spring.datasource.url=jdbc:mysql://localhost:3306/chess_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Peng148336099
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# 服务器
server.port=8081
server.servlet.context-path=/chess

# 日志配置
logging.level.root=INFO
logging.level.org.springframework.web=DEBUG
logging.file.name=logs/spring-boot-app.log

# 国际化
spring.messages.basename=messages
spring.messages.encoding=UTF-8
spring.messages.cache-duration=3600

# 文件上传
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Redis server address
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Redis password (if any)
#spring.data.redis.password=your_redis_password

# Redis connection timeout (in milliseconds)
spring.data.redis.timeout=60000

# Redis pool configuration
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0
spring.data.redis.lettuce.pool.max-wait=10000

# 阿里云OSS配置
# 对象存储桶的名字
oss.bucket=bilimili

# 外网访问的域名，记得最后面的"/"
oss.bucketUrl=https://bilimili.oss-cn-beijing.aliyuncs.com/

# 地域节点
oss.endpoint=http://oss-cn-beijing.aliyuncs.com

# 有访问权限的用户的 access-key-id
oss.accessKeyId=LTAI5tCdyKJvwVooJUFC8EAe

# 有访问权限的用户的 access-key-Secret
oss.accessKeySecret=DcHHLcQcrgSIFT4RaLeRyZeKa8INsT

# ossClient实例维持的空闲时间，单位毫秒，超过会自动关闭释放资源
oss.idleTimeout=10000